
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function AttributesPage() {
  return (
    <div>
        <Card>
            <CardHeader>
                <CardTitle>Attributes</CardTitle>
                <CardDescription>Manage product attributes like size, color, etc.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>Attribute management interface coming soon.</p>
            </CardContent>
        </Card>
    </div>
  );
}
